/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Creacion2ProgramaService } from './creacion2Programa.service';

describe('Service: Creacion2Programa', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Creacion2ProgramaService]
    });
  });

  it('should ...', inject([Creacion2ProgramaService], (service: Creacion2ProgramaService) => {
    expect(service).toBeTruthy();
  }));
});
